import { createMuiTheme } from "@material-ui/core/styles";

const palette = {
  primary: { main: "#546E7A" },
  secondary: { main: "#FF8A80" }
};
const themeName = "Cutty Sark Vivid Tangerine Gorilla";

export default createMuiTheme({ palette, themeName });
